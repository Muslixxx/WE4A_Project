<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {# Bloc pour le titre de la page (surchargeable) #}
    <title>{% block title %}Moodle Simplifié{% endblock %}</title>

    {# ✅ Google Fonts utilisés dans tout le site #}
    <link href="https://fonts.googleapis.com/css2?family=Comic+Neue&family=Luckiest+Guy&display=swap" rel="stylesheet">

    {# ✅ Favicon minimaliste personnalisé en SVG #}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {# ✅ Inclusion de Bootstrap 5 en CDN pour la mise en page rapide #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {# ✅ Inclusion du CSS personnalisé de l'application #}
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

    {# Style inline pour définir la couleur de fond du body #}
    <style>
        body {
            background-color: #80bfec; /* bleu ciel doux */
        }
    </style>

    {# Bloc extensible pour ajouter d'autres styles spécifiques par page #}
    {% block stylesheets %}
        <style>
            body {
                margin: 0;
                padding: 0;
            }
            {# Gestion spécifique si la page est fixe ou scrollable selon la variable `fixed_page` #}
            {% if fixed_page is defined and fixed_page %}
            html, body {
                height: 100%;
                overflow: hidden;
            }
            {% else %}
            html, body {
                overflow: auto;
            }
            {% endif %}
        </style>
    {% endblock %}
</head>

<body>
{# Affichage conditionnel de la navbar (peut être désactivé si besoin) #}
{% if show_navbar is not defined or show_navbar %}
    {% include 'partials/navbar.html.twig' %}
{% endif %}

{# Conteneur principal du contenu dynamique de la page #}
<div class="page-content" style="padding-top: 70px;">
    {% block body %}{% endblock %}
</div>

{# Bloc extensible pour les javascripts spécifiques par page #}
{% block javascripts %}
    {# Importation dynamique via importmap (gère les modules ES6 dans Symfony) #}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}
</body>

{# ✅ Inclusion du bundle Bootstrap 5 JavaScript (nécessaire pour dropdowns, modals, etc.) #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</html>
