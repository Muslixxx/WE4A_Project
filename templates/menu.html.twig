{% extends 'base.html.twig' %}

{% block title %}Menu Principal{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Menu Principal</h1>
        <div class="text-center">
            <p>Bienvenue sur Moodle Simplifié, vous êtes connecté !</p>
            <div class="mt-4">
                <a href="{{ path('ue') }}" class="btn btn-primary me-2">UE_exemple</a>
                <a href="{{ path('gestion') }}" class="btn btn-secondary me-2">Gestion du Compte</a>
            </div>
            <div class="mt-4">
                <a href="{{ path('app_logout') }}" class="btn btn-danger">Déconnexion</a>
            </div>
        </div>
    </div>
{% endblock %}
